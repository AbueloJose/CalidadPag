/* * TÚ YA TIENES CÓDIGO AQUÍ para rellenar los modales
 * (Ej: 'btnEditarConvenio', 'btnEditarUsuario', 'btnEditarVacante').
 * * ESE CÓDIGO ESTÁ PERFECTO Y DEBE QUEDARSE AQUÍ.
 * * Si implementas el "Modo Oscuro", el código para 
 * manejarlo también iría aquí (para guardarlo en localStorage).
*/

document.addEventListener('DOMContentLoaded', function () {
    
    // --- CÓDIGO QUE YA TIENES (DEBE QUEDARSE) ---
    // Para rellenar el modal de Editar Usuario
    document.querySelectorAll('.btnEditar').forEach(button => {
        button.addEventListener('click', function () {
            // ... tu código de rellenar modal ...
        });
    });

    // Para rellenar el modal de Editar Convenio
    document.querySelectorAll('.btnEditarConvenio').forEach(button => {
        button.addEventListener('click', function () {
            // ... tu código de rellenar modal ...
        });
    });
    
    // Para rellenar el modal de Editar Vacante
    document.querySelectorAll('.btnEditarVacante').forEach(button => {
        button.addEventListener('click', function () {
            // ... tu código de rellenar modal ...
        });
    });

    // etc...

});